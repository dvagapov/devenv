apiVersion: v2
name: vector
description: Vector agent — container logs + Prometheus metrics → ClickHouse
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: vector
    version: "0.50.0"
    repository: https://helm.vector.dev

apiVersion: v2
name: kube-state-metrics
description: Kube State Metrics for local k3d cluster
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: kube-state-metrics
    version: "5.26.0"
    repository: https://prometheus-community.github.io/helm-charts

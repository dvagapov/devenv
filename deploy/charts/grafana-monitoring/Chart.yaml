apiVersion: v2
name: grafana-monitoring
description: Grafana dashboards, alert rules and other Grafana CRDs, deployed separately from the Grafana Helm install
type: application
version: 0.1.0
appVersion: "1.0.0"
